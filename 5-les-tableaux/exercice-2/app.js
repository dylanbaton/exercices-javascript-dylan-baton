let couleurs = ["bleu", "blanc", "rouge"];
console.table(couleurs)
console.log(`la longueur du tableau est ${couleurs.length}`)
couleurs.push("violet");
couleurs.push("vert");
console.table(couleurs)
console.log(`la longueur du tableau est ${couleurs.length}`)