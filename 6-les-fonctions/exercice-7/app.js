// Exercice 7 : Nombre pair
// > Créez une fonction `estPair()` qui prend un nombre en argument et qui renvoi `true` si il est pair ou `false` si il est impair.

function estPair() {
    if () {
        return  
   } else {
       
   }
}